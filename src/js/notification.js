function fetchNotification() {
    //fetches notification

    //notifications consist of server types, it could be an upload session too
}

function fetchSessionInfo() {
    //fetches session info

    //session info would contain logs of several sessions
    //These logs can be
}